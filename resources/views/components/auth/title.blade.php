<div class="text-center mb-6">
    <h1 class="text-2xl font-bold text-white">
        {{ $title }}
    </h1>

    @if(isset($subtitle))
        <p class="text-gray-400 mt-2">
            {{ $subtitle }}
        </p>
    @endif
</div>
