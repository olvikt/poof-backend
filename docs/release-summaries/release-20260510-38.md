# release-20260510-38

## Summary
- Re-implemented client proof photo lightbox as a reusable Blade component (`x-proof-photo-lightbox`) to avoid fragile inline Blade/Alpine nesting.
- Integrated the component only in the client orders list awaiting-confirmation card.
- Kept in-app modal behavior with keyboard/backdrop close, photo counter, close control, and prev/next navigation for 2+ photos.
- Preserved empty-state rendering (`Фотозвіт відсутній`) when no proof URLs are available.
- Extended client orders feature tests to cover the empty proof URL scenario.

## Validation
- `php artisan view:clear`
- `php artisan view:cache`
- `php artisan test tests/Feature/Client/ClientOrdersPaymentUxTest.php --stop-on-failure`
